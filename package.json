{
  "name": "fpt-prep",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "clean": "node -e \"['reports','screenshots'].forEach(d=>{try{require('fs').rmSync(d,{recursive:true,force:true})}catch{}})\"",
    "test:chromium": "npm run clean && cross-env BROWSER=chromium cucumber-js --profile chromium",
    "test:firefox": "npm run clean && cross-env BROWSER=firefox cucumber-js --profile firefox",
    "test:webkit": "npm run clean && cross-env BROWSER=webkit cucumber-js --profile webkit",
    "test": "npm run clean && concurrently --kill-others-on-fail \"npm run test:chromium\" \"npm run test:firefox\" \"npm run test:webkit\"",
    "report": "node generate-report.js",
    "lint": "eslint ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@cucumber/cucumber": "^10.9.0",
    "@playwright/test": "^1.58.2",
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3",
    "eslint": "^8.57.0",
    "multiple-cucumber-html-reporter": "^3.10.0",
    "winston": "^3.19.0"
  }
}
